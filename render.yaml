services:
  - type: web
    name: unitool-proxy
    runtime: node
    rootDir: app
    buildCommand: npm install && npm run build
    startCommand: node out/index.js
    envVars:
      - key: NODE_ENV
        value: production
    autoDeploy: true
